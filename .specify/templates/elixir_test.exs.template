defmodule {{module_name}}Test do
  use ExUnit.Case, async: true
  
  alias {{module_name}}
  
  describe "{{function_name}}/{{arity}}" do
    test "{{test_description}}" do
      # Arrange
      {{arrange_comment}}
      
      # Act
      {{act_comment}}
      
      # Assert
      {{assert_comment}}
    end
    
    test "handles invalid input" do
      # Test error conditions and edge cases
    end
    
    test "handles boundary conditions" do
      # Test limits, empty values, nil handling
    end
  end
  
  # Helper functions
  defp setup_{{context}} do
    # Setup test data and context
  end
end
